# v0.1 Lifecycle Specification

The lifecycle SHALL expose `DEVELOPMENT`, `INTERNAL_TEST`, `EMBODIMENT_TEST`, `PRE_BIRTH`, and `BORN`. Only `BORN` SHALL create official identity and life records. Transition to `BORN` SHALL be auditable, snapshot-aware, and protected against accidental test-data admission.

